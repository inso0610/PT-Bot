/* Optimized production build generated by CommandKit */
import {
  __commonJS,
  __require,
  init_esm_shims
} from "../../chunk-KSB4BMFD.mjs";

// src/commands/misc/ping.js
var require_ping = __commonJS({
  "src/commands/misc/ping.js"(exports, module) {
    init_esm_shims();
    var { SlashCommandBuilder } = __require("discord.js");
    module.exports = {
      data: new SlashCommandBuilder().setName("ping").setDescription("Pong! Gives you the bot's ping"),
      run: async ({ interaction, client, handler }) => {
        await interaction.deferReply({
          content: "Wait...",
          ephemeral: true
        });
        const reply = await interaction.fetchReply();
        const ping = reply.createdTimestamp - interaction.createdTimestamp;
        interaction.editReply(`Pong! Client: ${ping}ms | Websocket: ${client.ws.ping} ms`);
      },
      options: {
        devOnly: false,
        guildOnly: false,
        userPermissions: [],
        botPermissions: ["Administrator"],
        deleted: false
      }
    };
  }
});
export default require_ping();
