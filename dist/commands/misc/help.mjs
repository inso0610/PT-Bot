/* Optimized production build generated by CommandKit */
import {
  __commonJS,
  __require,
  init_esm_shims
} from "../../chunk-KSB4BMFD.mjs";

// src/commands/misc/help.js
var require_help = __commonJS({
  "src/commands/misc/help.js"(exports, module) {
    init_esm_shims();
    var { SlashCommandBuilder, EmbedBuilder } = __require("discord.js");
    module.exports = {
      data: new SlashCommandBuilder().setName("help").setDescription("Sends a list of all commmands and their function.").addSubcommand((subcommand) => subcommand.setName("commands").setDescription("Sends a list of all commmands and their function.")),
      run: async ({ interaction, client, handler }) => {
        const subcommand = interaction.options.getSubcommand();
        if (subcommand === "commands") {
          const helpEmbed = new EmbedBuilder().setTitle("Commands:").addFields(
            { name: "/help:", value: "Sends a list of all commmands and their function." },
            { name: "/ping:", value: "Gives you the bot's ping." },
            { name: "/suggest:", value: "Sends a suggestion." },
            { name: "/rps:", value: "Play rock paper scissors against <@339379137947107328>" }
          );
          interaction.reply({
            embeds: [helpEmbed],
            ephemeral: true
          });
        } else {
          interaction.reply({
            content: "Command failure. Try again.",
            ephemeral: true
          });
        }
      },
      options: {
        devOnly: false,
        guildOnly: false,
        userPermissions: [],
        botPermissions: ["Administrator"],
        deleted: false
      }
    };
  }
});
export default require_help();
