/* Optimized production build generated by CommandKit */
import {
  init_esm_shims
} from "../../chunk-YVAUBM4S.mjs";
import {
  __commonJS,
  __require
} from "../../chunk-JS3VUUZD.mjs";

// src/commands/mod-stuff/kick.js
var require_kick = __commonJS({
  "src/commands/mod-stuff/kick.js"(exports, module) {
    init_esm_shims();
    var { SlashCommandBuilder, EmbedBuilder } = __require("discord.js");
    module.exports = {
      data: new SlashCommandBuilder().setName("kick").setDescription("Kick's a user from the Discord server.").addUserOption((option) => option.setName("user").setDescription("Who do you want to kick?").setRequired(true)).addStringOption((option) => option.setName("reason").setDescription("What is the reason for kicking the user?").setRequired(true)),
      run: ({ interaction, client, handler }) => {
        const kickUser = interaction.options.getUser("user");
        const kickUserId = kickUser.id;
        const reason = interaction.options.getString("reason");
        interaction.reply(kickUserId);
      },
      options: {
        devOnly: false,
        userPermissions: ["Administrator"],
        botPermissions: ["Administrator"],
        deleted: true
      }
    };
  }
});
export default require_kick();
