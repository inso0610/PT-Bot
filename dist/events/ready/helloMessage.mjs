/* Optimized production build generated by CommandKit */
import {
  __commonJS,
  init_esm_shims
} from "../../chunk-KSB4BMFD.mjs";

// src/events/ready/helloMessage.js
var require_helloMessage = __commonJS({
  "src/events/ready/helloMessage.js"(exports, module) {
    init_esm_shims();
    module.exports = (client) => {
      const randomChat = client.channels.cache.get("1189740423741190226");
      const d = /* @__PURE__ */ new Date();
      const year = d.getFullYear();
      const month = d.getMonth();
      const months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
      const monthName = months[month];
      const date = d.getDate();
      const day = d.getDay();
      const days = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
      const dayName = days[day];
      const hour = d.getHours();
      const minute = d.getMinutes();
      randomChat.send(`**Hello!\u{1F44B}** *PT Assistance went online ${dayName} ${date} ${monthName} ${year} ${hour}:${minute}*`);
    };
  }
});
export default require_helloMessage();
